# NeuralBrief Render Blueprint
# Deploy: New > Blueprint > Connect repo > Select this file
# Only Postgres - run pipeline manually: python main.py (with DATABASE_URL in .env)

databases:
  - name: neuralbrief-db
    databaseName: neuralbrief
    plan: free
